#include <stdio.h>
/**
 * main	-programe that prints the size of various types on the computer
 * return:	show that the programme is successful
 */

int main(void)
{
	char x;
	int y;
	long int z;
	long long int a;
	float b;

printf("size of a char %lu byte(s)\n", (unassigned long)sizeof(z));
printf("size of a int %lu byte(s)\n", (unassigned long)sizeof(y));
printf("size of a long int %lu byte(s)\n", (unassigned long)sizeof(z));
printf("size of a long long int %lu byte(s)\n", (unassigned long)sizeof(a));
printf("size of a float %lu byte(s)\n", (unassigned long)sizeof(b));
return (0);
}
